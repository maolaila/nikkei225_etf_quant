from __future__ import annotations

from src.models.rule_based_model import RuleBasedModel


class XGBoostModel(RuleBasedModel):
    """Optional dependency placeholder that safely falls back to rule-based predictions."""

